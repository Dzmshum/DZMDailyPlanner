# План доработок PlanBoard

> Продукт: **DoomPlanner → RuneBoard → PlanBoard** (UI и `APP_NAME` с v0.25.1). Имя `.exe` и пути данных `%APPDATA%\DoomPlanner\` пока без изменений.

## Текущая версия: **v0.25.1** ✅

Ребрендинг **PlanBoard** + wordmark-логотипы per-палитра (v0.25.1). Ранее: палитры по вселенным (v0.24) + кастомная тема и «Классика» (v0.25).

- [x] **7 wordmark** — эмблема + «PlanBoard» в стиле каждой палитры; прозрачный фон; сайдбар и минимальное окно
- [x] Убраны текстовые «RuneBoard» и «Рунный планировщик задач» из UI
- [x] `APP_NAME` → `PlanBoard`; `<title>` и заголовок окна Electron
- [x] `public/icons/wordmark/` — обработка в `generate-icon.mjs`; проверка в `verify-palettes` (83) и `verify-icons` (57 PNG)
- [ ] `productName` / имя `.exe` → PlanBoard *(backlog)*

**v0.25:**
- [x] **7 палитр:** `plain` (Классика), WoW (Нордскол / Запределье / Пандария), Звёздные войны / Игра престолов / Ведьмак
- [x] Анимации фона per-палитра (MVP: звёзды / снег / моты); полноценные тематические эффекты → **v0.27**
- [x] **Своя тема:** color picker (акцент, фон, панели, текст), фоновое изображение, экспорт/импорт JSON, сброс к «Классика»
- [x] Новые планы и сброс оформления → `colorPalette: plain`, без WoW-декора
- [x] `verify-palettes.mjs` в `pnpm test`; иконки для всех палитр (`pnpm icons`)

---

## Дальше по плану

| # | Версия | Суть |
|---|--------|------|
| **1** | **v0.23** | **Мобильное приложение** — тот же план на телефоне (см. варианты синхронизации) |
| **2** | **v0.26** | **Дейлик:** объединять в отчёте очень похожие задачи по смыслу (одна строка вместо дублей) |
| **3** | **v0.27** | **Анимации фона 2.0** — тематические эффекты для SW / GoT / Ведьмак + полировка WoW-палитр |
| **4** | **v0.3** | Ollama — короткий заголовок из текста, подсказка проекта; *опционально — семантика для v0.26* |

*К следующим версиям — см. [«Каждая новая версия — обязательно»](#каждая-новая-версия--обязательно): оптимизация + тесты. Выпущенные версии — в [истории](#история-версий).*

**Полировка UI (backlog):**
- [x] Отдельные прозрачные иконки per-палитра (8 вкладок, WoW-стиль)
- [x] **UI-иконки** — окно, закрыть, шевроны (`public/icons/ui/`, `UiIcon`)
- [x] **История** — группировка выполненных задач по дате (`completedAt`)
- [x] Иконки в Electron (`assetUrl`, `resources/icon.png`, `verify-icons.mjs`)
- [x] Календарь РФ 2025–2027: `build-production-calendar.mjs`, ПП №1335 / №1466 / проект 2027; fallback 2028+
- [x] Дейлик: read-only список, приглушённый блок «Не сделано»
- [x] Календарь: full-height, «Сегодня», вкладка «Календарь», месяц/неделя UI
- [x] **Скроллбары** — кастомный стиль под палитру и тему
- [x] **Сборка Electron** — source PNG не в пакете; автопроверка иконок
- [x] **Завершённые проекты** — см. [v0.22.5](#v0225--завершённые-проекты)
- [ ] Анимация перехода между палитрами *(→ [v0.27](#v027--анимации-фона-20-по-новым-темам))*
- [ ] Тематические фоновые эффекты SW / GoT / Ведьмак *(базовые частицы есть в v0.24 → v0.27)*
- [ ] Vitest для `exportPlanText` и `dailyMeetings` *(пока `pnpm test` = calendar + verify-daily)*
- [ ] **Голосовой ввод** — проблема **актуальна**: в Electron не работает / нестабильно; `[Voice]` в F12, fallback или нативный API
- [ ] Импорт/экспорт JSON **с файлами вложений** (сейчас только метаданные в JSON)
- [x] Календарь РФ на 2027+ *(2027 — проект ПП; 2028+ — приблизительно по ст. 112 ТК РФ)*
- [ ] **Дейлик:** схлопывать очень похожие задачи в одну строку отчёта *(→ v0.26)*
- [ ] Полный ребренд: `productName` / `.exe` → PlanBoard; пути `%APPDATA%\DoomPlanner\` → PlanBoard

**Идеи на потом:** PDF/Markdown экспорт, уведомления Windows, удалить `src-tauri/`.

---

## Каждая новая версия — обязательно

Помимо фич версии, **в каждом релизе** (v0.17, v0.19, v0.2 …):

| Блок | Что делать |
|------|------------|
| **Оптимизация** | Пройтись по затронутому коду и приложению в целом: bundle size, лишние ре-рендеры, тяжёлые эффекты (canvas, анимации), дубли CSS, мёртвый код |
| **Тесты** | Добавить/обновить тесты под новый функционал; прогнать весь чеклист из [`TESTS.md`](TESTS.md) |

**Чеклист перед закрытием версии (v0.17–v0.22.6):**
- [x] `pnpm build` успешен
- [x] `pnpm electron:build` успешен *(v0.22.2)*
- [x] `pnpm test` — calendar + daily + attachments + month + holidays + projects + palettes *(v0.25.1: 83)*
- [ ] `pnpm lint` без новых предупреждений *(есть warnings в scripts)*
- [ ] Ручной прогон пунктов из `TESTS.md` для изменённых областей
- [ ] В `TESTS.md` добавлены строки под новые фичи
- [x] Краткая заметка в ROADMAP *(этот блок)*

**Оптимизация v0.17–v0.22.2:**
- Общие lib-модули: `holidays`, `dailyMeetings`, `exportPlanText`, `assetUrl`
- `pnpm calendar` → `build-production-calendar.mjs`; `verify-daily-meetings.mjs` для дейликов
- `strip-icon-sources.mjs` — ~80 МБ `*-source.png` не попадают в `build/` и .exe
- `verify-icons.mjs` — 57 PNG + `resources/icon.png` перед electron-builder
- Единый `AppShell` для модалок; устойчивый `electron-build.mjs` при EPERM
- Иконка Electron вынесена в `resources/icon.png` (Vite не перезаписывает)

> Список тестов живой — **обновлять `TESTS.md` при каждой новой фиче**, не только в конце версии.

---

## История версий

Выпущенные релизы. Актуальная версия — [v0.25.1](#текущая-версия-v0251-) (см. выше).

| Версия | Статус | Содержание |
|--------|--------|------------|
| v0.11 | ✅ | Меню, кнопки окна, импорт JSON, путь к данным |
| v0.12 | ✅ | Удаление с карточек и из истории |
| v0.13 | ✅ | Стиль «лёд / корона» |
| v0.14 | ✅ | Улучшенная модалка задачи |
| v0.15 | ✅ | Иконка + кнопка «выполнено» |
| v0.16 | ✅ | Повестка: подсписок «Выполнено» |
| v0.17 | ✅ | Минималистичный режим окна **(Electron)** |
| v0.18 | ✅ | WoW-палитры: Нордскол, Запределье, Пандария |
| v0.19 | ✅ | Праздники РФ + месяц / квартал / год |
| v0.2 | ✅ | Входящие, quick capture, drag на дату |
| v0.20 | ✅ | Текстовая выгрузка: день / неделя / месяц / квартал / год |
| v0.21 | ⚠️ | Голосовой ввод (UI есть, в Electron нестабилен) |
| v0.22 | ✅ | Дейлики: логика периода, календарь, отчёт |
| v0.22.1 | ✅ | Полировка: производственный календарь, UI дейлика/календаря, RuneBoard |
| v0.22.2 | ✅ | UI-иконки WoW, голос во всех полях, verify-icons + strip sources |
| v0.22.3 | ✅ | Фото к задачам: вложения, лайтбокс, Ctrl+V, тесты |
| v0.22.4 | ✅ | Месяц: мини-полоски задач; дейлик 13:00 + прошлые дейлики |
| v0.22.5 | ✅ | Завершённые проекты: статус, иконка, скрытие в выборе |
| v0.22.6 | ✅ | Календарь РФ 2027 (проект ПП) + fallback 2028+ |
| v0.24 | ✅ | Палитры SW / GoT / Witcher + анимации фона — [детали](#v024--палитры-по-вселенным--анимации-фона) |
| v0.25.1 | ✅ | Ребрендинг PlanBoard: wordmark per-палитра, UI без текста RuneBoard — [детали](#v0251--ребрендинг-planboard-wordmark) |
| v0.25 | ✅ | Кастомная тема + «Классика» по умолчанию — [детали](#v025--кастомная-тема-пользователя) |

### Детали

### MVP (v0.1) — готово

- Дашборд, повестка, неделя, задачи, история
- Проекты, дедлайны, приоритет, заметки
- Drag-and-drop дедлайнов на недельном календаре
- Jira Cloud export, темы, автосохранение JSON
- Electron .exe, portable-сборка

---

### v0.18 — WoW-палитры ✅

Три палитры по мотивам локаций WoW — **только цвета и формы**, без персонажей.

| Палитра | Референс | Цвета и мотивы |
|---------|----------|----------------|
| **Нордскол** | Ледяная Корона, Frostmourne | Чёрно-синий фон, электрический cyan `#00e5ff`, сталь, руническое свечение, острые углы |
| **Запределье** | Иллидан, Outland | Тёмный фиолет, скверна-зелёный `#7cfc00`, трещины, fel-glow |
| **Пандария** | Pandaria | Нефрит `#3daa6a`, золото, тёплый шёлк, лёгкий розовый акцент сакуры |

**Сделано:**
- [x] Переключатель в ⚙ Настройки → Оформление
- [x] `data-palette` на `<html>`, сохранение в `plan.json`
- [x] Нордскол по референсам Цитадели / Frostmourne
- [x] Руна-кнопка «Готово» с выезжающей подписью при наведении

**Доработать (backlog):**
- [x] UI-иконки per-палитра (окно, закрыть, шевроны) — v0.22.2
- [ ] Анимации перехода между палитрами
- [ ] Отдельные иконки приложения под каждую тему (опционально)

---

### v0.15 — иконка и кнопка «выполнено» ✅

- SVG-иконка (цитадель / руны), `pnpm icons` → `public/icon.png`
- Кастомный `TaskCompleteToggle`: руническая пластина, галочка, анимация свечения
- Состояние «в работе» — золотое кольцо
- `aria-checked`, фокус с клавиатуры

**Критерии:**
- [x] Иконка читается в трее и на рабочем столе
- [x] Отметка задачи заметная и в стиле приложения
- [x] Не системный checkbox

---

### v0.16 — повестка: выполненные за день ✅

- Секция **«К выполнению»** — активные задачи на дату
- Секция **«Выполнено»** — свёртываемый подсписок, группировка по проектам
- Приглушённые карточки; снятие галочки → обратно в активные
- Счётчик в меню — только активные (без изменений)

**Критерии:**
- [x] Активные и выполненные на выбранную дату
- [x] Визуально разделены
- [x] Снятие галочки возвращает в активные
- [x] Навигация по датам обновляет оба списка

---

### История: группировка по дате ✅ *(v0.22)*

- [x] Секции «Сегодня», «Вчера», полная дата
- [x] Группировка по `completedAt` (fallback `createdAt`)
- [x] Поиск фильтрует группы; пустое «ничего не найдено»
- [x] Sticky-заголовки секций

---

### Скроллбары ✅ *(v0.22.1)*

- [x] Тонкий скролл (~8px), без стрелок, цвет из `--accent` / `--bg-tertiary`
- [x] `::-webkit-scrollbar` + `scrollbar-color` (Firefox)
- [x] Модалка: скролл только в `.modal-body`; календарь — `position: fixed`, без лишнего скролла

**Критерии:**
- [x] Нет «белых» системных полос в тёмной теме
- [ ] Проверить на длинной истории и в Electron после релиза
- [x] При смене палитры скролл наследует акцент

---

### v0.17 — минималистичное окно ✅ *(Electron)*

> **Только десктоп (Electron).** В браузере (`pnpm dev`) режим не доступен.

- [x] Режимы `standard` | `maximized` | `minimal` в `settings.windowMode`
- [x] IPC: `set-window-mode`, `get-window-mode`, `expand-to-standard`
- [x] Минимальный UI (`MinimalView`): сегодня + входящие, `+`, `Q`, «Развернуть»
- [x] ~320×480, `alwaysOnTop`, правый верхний угол
- [x] Кнопки окна — WoW-иконки (`UiIcon`: компакт / развернуть / макс / закрыть)
- [x] Переключатель в ⚙ → Оформление → «Режим окна»; режим сохраняется между запусками

---

### v0.19 — календарь: праздники и разрезы ✅ *(доработано в v0.22.1)*

- [x] Праздники РФ (`holidays-ru-2025/2026/2027.json`), вкл/выкл в настройках
- [x] **Производственный календарь** — `scripts/build-production-calendar.mjs`, `pnpm calendar`; 247 рабочих / 118 выходных; переносы ПП №1335 (2025) / №1466 (2026) / проект 2027; рабочие субботы 01.11.2025, 20.02.2027; стыки годов; fallback 2028+ (`holidayFallback.ts`)
- [x] Переключатель: Календарь (неделя) | Месяц | Квартал | Год
- [x] Месяц — сетка, счётчики, дейлик как событие ◆, клик → повестка
- [x] Квартал / год — heatmap; кнопка «Сегодня» на всех разрезах
- [x] Full-height layout; колонки недели со скроллом внутри
- [x] DnD на неделе; панель «Без даты»; выполненные приглушены

**Не в MVP:** региональные праздники, DnD на месячном виде. ~~JSON на 2027+~~ — сделано в v0.22.6 (2027 официальный проект; 2028+ приблизительно).

---

### v0.2 — быстрый захват и «входящие» ✅

- [x] Вкладка «Входящие» + счётчик в сайдбаре
- [x] `Q` — быстрый захват (`QuickCaptureModal`)
- [x] Панель «Без даты» на экране недели
- [x] Drag inbox → день недели; drag → «Без даты» снимает deadline
- [x] Дашборд корректен для `deadline: null`
- [x] Экспорт/импорт JSON сохраняет `deadline: null`

---

### v0.20 — текстовая выгрузка плана (Telegram) ✅

- [x] `src/lib/exportPlanText.ts`
- [x] Кнопка «Текст TG», `Ctrl+Shift+C`
- [x] Периоды: день / неделя / месяц / квартал / год
- [x] Превью, копирование, сохранение `.txt`
- [x] Настройки: включать выполненные, пропускать пустые дни, заголовок
- [x] Формат `DD/MM:` и `- задача`; UTF-8

**Backlog:** группировка диапазонов `DD/MM - DD/MM`; Vitest на формат строк.

---

### v0.21 — голосовой ввод ⚠️ частично

- [x] Web Speech API (`ru-RU`), вкл/выкл в настройках (`settings.voiceInputEnabled`)
- [x] `VoiceInputField` — название, заметки (`multiline`), проекты, быстрый захват; `Ctrl+Shift+V`
- [x] Hotkey на поле с фокусом (или первое в модалке)
- [x] Interim-текст, логи `[Voice]` в консоли (F12), ошибка в UI

**Backlog (актуально):**
- [ ] Голос **не работает** у пользователя в Electron — разобрать по логам `[Voice]`, проверить разрешение микрофона, `webkitSpeechRecognition` в Chromium
- [ ] Стабильный сценарий: старт → interim → финальный текст в поле; повторное нажатие останавливает запись
- [ ] Понятное сообщение в UI, если API недоступен (браузер без Speech / блокировка ОС)
- [ ] Ручной чеклист в `TESTS.md` после исправления

---

### v0.22 — дейлики и отчёт «между дейликами» ✅ *(UI в v0.22.1)*

- [x] `src/lib/dailyMeetings.ts` — пн/чт, только рабочие дни, учёт производственного календаря
- [x] Логика периода: закрытое **до дня дейлика** (вкл. день прошлого дейлика); в день дейлика → следующий отчёт
- [x] `scripts/verify-daily-meetings.mjs` — 23 автопроверки
- [x] Отметки ◆ на недельном и месячном календаре
- [x] Вкладка «Дейлик»: группировка по проектам, «Скопировать для дейлика»
- [x] **Read-only список** — название + заметки (для зачитывания на созвоне)
- [x] «Не сделано» — свёрнуто по умолчанию, приглушённый стиль

**Backlog:** настройка произвольных дней недели в UI (сейчас только вкл/выкл, дни `[1, 4]`).

---

### v0.22.1 — полировка UI и бренд ✅

- [x] Переименование **DoomPlanner → RuneBoard** (UI, `productName`, `APP_NAME`) — superseded **PlanBoard** в v0.25.1
- [x] Вкладка «Проекты» (`ProjectsView`), настройки `modal-xl`
- [x] Иконки inbox / daily / projects — AI PNG по референсу WoW-вкладок
- [x] Входящие: `Q`/`Й`, drag-reorder, полоса недели с выходными
- [x] Скроллбары, DatePicker без лишнего скролла модалки

---

### v0.22.3 — фото к задачам ✅

- [x] `TaskAttachment` в модели; `normalizePlan` → `attachments: []` для старых планов
- [x] Хранение: `electron/attachments-file.cjs`, IPC `save/read/delete-attachment`, dev API
- [x] `attachmentStorage.ts` — сжатие JPEG, кэш blob URL, IndexedDB fallback (preview без Electron)
- [x] `TaskAttachments` — файл, drag-and-drop, глобальный `Ctrl+V` (`useClipboardImagePaste`)
- [x] Задача без названия, если есть фото (`resolveTaskTitle` / `canSaveTask`)
- [x] `ImageLightbox` — portal в `document.body`, блокировка скролла, `suppressClickThrough`
- [x] Миниатюры на `TaskCard`; отмена формы → `cleanupDraftAttachments`
- [x] `verify-attachments.mjs` в `pnpm test`

**Backlog:** bundle вложений в экспорт JSON; превью в дейлике / Telegram-экспорте.

---

### v0.22.2 — UI-иконки, голос, Electron ✅

- [x] `UiIcon` + `public/icons/ui/{palette}/` — компакт, развернуть, макс, закрыть, шевроны
- [x] Замена Unicode (▢, ✕, ▶) на PNG в стиле палитры
- [x] Голос во всех текстовых полях (`VoiceInputField` + textarea)
- [x] `strip-icon-sources.mjs` — исходники AI не в production build
- [x] `verify-icons.mjs` — проверка 50 assets перед `electron-builder`
- [x] `pnpm electron:build` — portable + NSIS без ошибок иконок

---

### v0.22.4 — месяц и дейлик ✅

- [x] **Месячный календарь** — мини-полоски задач (динамический лимит + `+N`), цвет проекта, клик → редактирование
- [x] **Дейлик** — отсечка 13:00, переключение отчёта после созвона, просмотр прошлых дейликов
- [x] **Тесты** — `verify-month-calendar.mjs` (9 проверок)

---

### v0.22.5 — завершённые проекты ✅

Отметка проекта как **«сделан»** (архивный): не мешает в ежедневной работе, но остаётся в истории и на старых задачах.

**Модель данных (`Project` в `plan.json`):**
- [x] Поле `completed: boolean` (по умолчанию `false`; `normalizePlan` для старых планов)
- [x] Опционально `completedAt: string | null` — когда отметили (для сортировки в списке проектов)

**Вкладка «Проекты» (`ProjectManager`):**
- [x] Переключатель или кнопка «Проект завершён» / «Вернуть в работу»
- [x] Завершённые — отдельная секция внизу или фильтр «Активные / Завершённые»
- [ ] Завершённый проект **нельзя удалить** одним кликом без подтверждения, если к нему привязаны задачи *(как сейчас, но явнее)*

**Визуальное отличие:**
- [x] Цвет проекта приглушён (opacity / desaturate), иконка ✓ или «архив» на строке проекта
- [x] На карточках задач и в календаре — полоска проекта остаётся узнаваемой, но приглушённой
- [x] В группировках (повестка, дейлик) — без изменений логики, только стиль бейджа

**Выбор проекта при создании задачи (`ProjectSelect` / форма, быстрый захват):**
- [x] По умолчанию в списке — только **активные** проекты + «Без проекта»
- [x] Завершённые **не показываются сверху** и не попадают в выпадающий список без поиска
- [x] При **вводе текста** (combobox / поиск по названию) — подбираются и активные, и завершённые (чтобы можно было вручную привязать задачу к старому проекту)
- [x] Уже выбранный завершённый проект в **редактировании** задачи отображается корректно (не пропадает из поля)

**Селекторы и счётчики:**
- [x] `getActiveProjects()` — для UI выбора; завершённые исключены
- [x] Счётчик вкладки «Проекты» — только активные *(или активные + подпись «N завершено»)*

**Тесты:**
- [x] `scripts/verify-projects.mjs` — `normalizePlan`, фильтр активных, поиск по имени с завершёнными
- [x] Пункты в `TESTS.md` §3 (проекты) и §2 (форма задачи)

**Критерии готовности:**
- [x] Новая задача: в списке проектов нет завершённых, пока не начал вводить название
- [x] Завершённый проект визуально отличим на вкладке «Проекты»
- [x] Старые задачи с `projectId` завершённого проекта отображаются как раньше

---

## Планы будущих версий

### v0.24 — палитры по вселенным + анимации фона ✅

Новые готовые темы (цвета + формы + фон), по аналогии с WoW-палитрами v0.18:

| Палитра | Референс | Мотивы |
|---------|----------|--------|
| **Звёздные войны** | Светлая / тёмная сторона | Глубокий космос, неон синий/красный, имперский серый |
| **Игра престолов** | Вестерос | Тёмный гранит, бордо, золото, снег Севера |
| **Ведьмак** | Continent | Медь, мутная зелень, пергамент, руны |

**Анимации фона** *(MVP в v0.24 — простые частицы; полноценные тематические эффекты → [v0.27](#v027--анимации-фона-20-по-новым-темам))*:
- [x] Базово: звёзды (SW), снег (GoT), моты (Ведьмак); переключатель «По палитре / Выключить»
- [ ] Гиперпространство, искры Силы, падающий снег Севера, руны/туман и т.д.
- [ ] Пульсация свечения палитры (`--accent-glow`, `--rune-glow`)
- [ ] Выбор пресета анимации в настройках и в «Своей теме»

**Критерии:**
- [x] 3+ новых палитры в переключателе
- [x] `AmbientBackground` поддерживает режим анимации из settings
- [x] Плавное переключение без лагов в Electron

---

### v0.25.1 — ребрендинг PlanBoard (wordmark) ✅

- [x] Название продукта в UI: **PlanBoard** (`APP_NAME`, `<title>`, заголовок окна Electron)
- [x] Убраны подписи «RuneBoard» и «Рунный планировщик задач» из сайдбара
- [x] Компактные **wordmark** для 7 палитр: эмблема + «PlanBoard», прозрачный фон
- [x] `BrandMark variant="wordmark"` — сайдбар (`lg`) и минимальное окно (`sm`)
- [x] `public/icons/wordmark/{palette}-source.png` → `{palette}.png`; квадратная эмблема для карточек палитр и `resources/icon.png`
- [x] `verify-palettes.mjs` — +7 проверок wordmark (83 всего); `verify-icons.mjs` — wordmark в build

**Backlog:** `productName` / portable `RuneBoard.exe` → PlanBoard.

---

### v0.25 — кастомная тема пользователя ✅

- **По умолчанию** — обычная **тёмная тема**: нейтральные серо-чёрные тона, без WoW-палитры, без декоративного фона и анимации *(для новых планов и сброса оформления)*
- Редактор: основной / акцентный / фоновый цвет (color picker)
- Загрузка фонового изображения (Electron: файл; браузер: drag-n-drop)
- Выбор анимации фона из пресетов *(частично: `customTheme.ambientEnabled`; полный каталог → v0.27)*
- Сохранение в `plan.json` → `settings.customTheme`
- Экспорт/импорт темы как JSON

**Критерии:**
- [x] Новый план открывается в классической тёмной теме, не в WoW-палитре
- [x] Пользовательская тема не ломает читаемость текста
- [x] Сброс к теме по умолчанию одной кнопкой

---

### v0.23 — мобильное приложение

Нужна **мобильная версия** для просмотра и быстрого редактирования плана с телефона. Идеал — **один источник данных** с десктопом; минимум — надёжный **экспорт / импорт** без потери.

#### Варианты интеграции (рекомендация)

| Вариант | Суть | Плюсы | Минусы | Когда выбирать |
|---------|------|-------|--------|----------------|
| **A. PWA** | Адаптивный UI + «Установить на экран» из браузера | Один код с Vite/React, быстрый MVP | Нет доступа к файловой системе ОС; синк только вручную или через облако | Проверить UX на телефоне за 1–2 спринта |
| **B. Capacitor** ⭐ | Обёртка над тем же React-бандлом | ~90% общий код, доступ к файлам / Share API, можно в Store | Нужна отдельная сборка iOS/Android | **Оптимальный баланс** для DoomPlanner |
| **C. React Native / Expo** | Отдельное мобильное UI | Нативнее, жесты | Дублирование UI; общее только `types` + селекторы | Если PWA/Capacitor не устроят по UX |
| **D. Только синк-файл** | Десктоп + мобилка читают один `plan.json` в облачной папке | Один файл = один источник правды | Конфликты при одновременном редактировании | Как **фаза 1** параллельно с A или B |

**Рекомендуемый путь:** **B (Capacitor)** + поэтапная синхронизация:

1. **Фаза 1 — файловый обмен (уже почти есть):** импорт/экорт JSON, «Поделиться файлом» на телефоне, опционально путь к `plan.json` в папке OneDrive / Google Drive / iCloud (watch + merge при открытии).
2. **Фаза 2 — WebDAV / облачная папка:** настройка URL + автосинк при старте и по таймеру; стратегия merge: last-write-wins или ручной выбор при конфликте.
3. **Фаза 3 — свой бэкенд (опционально):** лёгкий API + аккаунт; только если нужен real-time между несколькими устройствами.

#### Общий код (monorepo, позже)

```
packages/
  core/          ← types, selectors, exportPlanText, dailyMeetings
apps/
  desktop/       ← Electron (текущий проект)
  mobile/        ← Capacitor shell + общий src или symlink
```

Либо без monorepo на старте: вынести `src/lib/` и `src/types/` в `@doomplanner/core` позже.

#### Мобильный MVP (v0.23)

- Адаптивная вёрстка: дашборд, повестка, отметка «готово», входящие
- Чтение/запись `plan.json` через Capacitor Filesystem или импорт из «Файлы»
- Те же WoW-палитры и темы (упрощённый сайдбар → bottom nav)
- **Не в MVP:** drag на календаре, Jira, Ollama, минималистичное окно Electron

**Критерии:**
- [ ] Приложение открывается на Android *(iOS — по возможности)*
- [ ] План загружается из JSON (файл или импорт)
- [ ] Изменения сохраняются и подхватываются на десктопе через экспорт/импорт или общую папку
- [ ] Критичные экраны usable на экране 360px

---

### v0.11–v0.14 — UX, окно, данные, визуал ✅

| Версия | Содержание |
|--------|------------|
| v0.11 | Скрытое меню Electron, кнопки окна, импорт JSON, путь к данным, `.bak` |
| v0.12 | Удаление с карточек и из истории, ConfirmDialog |
| v0.13 | Стиль «лёд / корона» (предшественник WoW-палитр) |
| v0.14 | Модалка задачи: чипы дедлайна, Enter, «Подробнее», «Без даты» |

---

---

### v0.26 — дейлик: группировка похожих задач

В отчёте «Скопировать для дейлика» и на вкладке **Дейлик** — не выводить подряд десяток почти одинаковых формулировок, а **объединять по смыслу** в одну группу с общим заголовком.

**Пример:** «Поправить баг в логине», «Фикс авторизации», «Login bug» → одна строка: **«Авторизация / логин»** (3 задачи) или список под одним заголовком.

#### Подход (поэтапно)

| Фаза | Как | Зависимости |
|------|-----|-------------|
| **MVP** | Нормализация текста + fuzzy match (Levenshtein, общие токены, тот же проект) | Без ИИ |
| **Улучшение** | Embeddings / запрос к **Ollama** — кластеризация и общий ярлык группы | v0.3 (опционально) |

#### Поведение

- Группировка только в **UI дейлика** и в **тексте для копирования** — исходные задачи в плане не сливаются
- Порог «очень похожие» — настраиваемый (строго / умеренно) или авто с консервативным дефолтом
- Внутри группы — перечисление оригинальных названий или счётчик `(×3)`
- Учитывать **проект**: не смешивать разные проекты без явного совпадения текста
- Ручное «разъединить группу» в отчёте — backlog, не блокер MVP

**Критерии:**
- [ ] В отчёте за период дейлика 5+ похожих задач отображаются компактнее, чем 5 отдельных строк
- [ ] «Скопировать для дейлика» отражает те же группы
- [ ] Логика периода / 13:00 / прошлые дейлики **не ломается** (`verify-daily-meetings.mjs`)
- [ ] Тесты на `groupSimilarDailyTasks()` (или аналог) в `pnpm test`

---

### v0.27 — анимации фона 2.0 по новым темам

В v0.24 заложены **цвета** и **базовые частицы** (точки / снежинки / моты). Здесь — **узнаваемые тематические эффекты** для вселенских палитр и полировка WoW-анимаций. «Классика» (`plain`) по-прежнему без декора.

#### Каталог эффектов по палитре

| Палитра | Сейчас (v0.24) | План v0.27 |
|---------|----------------|------------|
| **Звёздные войны** | Мелкие звёзды | Слои параллакса; редкие «гиперпрыжки» (полосы); вспышки красного/синего неона; лёгкая пульсация «двойного солнца» в `--bg-frost` |
| **Игра престолов** | Снежинки | Плотный снег Севера; золотые искры (как факелы); редкие бордовые «листья» / пепел; зимой усиленный снег |
| **Ведьмак** | Моты вверх | Пар / туман снизу; светящиеся **руны** (символы); медные искры; медленное «свечение знака» по центру экрана |
| **Нордскол** | Снежинки | Иней по краям canvas; более выразительный блеск льда |
| **Запределье** | Искры вверх | Скверна-пульсация, фиолетовые вспышки, зелёное пламя |
| **Пандария** | Лепестки | Ветер, нефритовые блики, золотые частицы |

#### Настройки (`settings.ambientPreset`)

| Значение | Поведение |
|----------|-----------|
| `auto` | Эффект по выбранной палитре *(дефолт)* |
| `off` | Без canvas-анимации |
| `subtle` | Меньше частиц, ниже opacity |
| `intense` | Больше частиц *(осторожно с CPU)* |

В **«Своей теме»** — выбор пресета из списка (не только вкл/выкл).

#### Техника

- Расширить `AmbientBackground`: новые `kind` частиц (`streak`, `rune`, `fog`, `spark`)
- Профили в `ambientProfiles.ts` (вынести из компонента)
- `requestAnimationFrame` + лимит FPS при свёрнутом окне / `document.hidden`
- `prefers-reduced-motion: reduce` → только статичный `--bg-frost`
- Плавный **crossfade** canvas при смене палитры (backlog из UI-полировки)
- Не блокировать UI: один canvas, DPR ≤ 2, пауза вне viewport

**Критерии:**
- [ ] У каждой вселенской палитры (SW / GoT / Ведьмак) эффект визуально отличим от WoW и друг от друга
- [ ] Переключение палитры без заметного фриза (>100 ms) на типичном ПК
- [ ] `ambientPreset` сохраняется в `plan.json`, работает с `customTheme`
- [ ] `verify-ambient.mjs` (или расширение `verify-palettes`) — профиль на каждую палитру
- [ ] Electron + браузер: одинаковое поведение

---

### v0.3 — локальный ИИ (Ollama)

- **Заголовок из текста** — при быстром захвате
- **Подсказка проекта** — по смыслу текста
- **Семантическая группировка для дейлика** *(опционально, усиливает [v0.26](#v026--дейлик-группировка-похожих-задач))* — общий ярлык кластера похожих задач
- Настройки: URL Ollama, модель, вкл/выкл
- Вызов через Electron main process (как Jira)

---

### v0.x — прочее (идеи)

| Приоритет | Задача |
|-----------|--------|
| Средний | PDF экспорт / Markdown (отличный от v0.20 Telegram-текста) |
| Низкий | Удалить или заархивировать `src-tauri/` |
| Низкий | Синхронизация browser ↔ desktop через импорт JSON *(→ v0.23)* |
| Низкий | Напоминания / уведомления Windows |
| Низкий | Real-time sync (собственный сервер) — только после v0.23 фазы 1–2 |

---

### Связь с текущим кодом

| Модуль | Статус |
|--------|--------|
| `WeekCalendar` + inbox DnD | ✅ inbox → day, reorder, полоса недели |
| `InboxView`, `QuickCaptureModal` | ✅ вкладка + `Q` / `Й` |
| `CalendarView` (неделя/месяц/квартал/год) | ✅ + производственный календарь РФ |
| `build-production-calendar.mjs` | ✅ генерация `holidays-ru-*.json` |
| `exportPlanText.ts`, `ExportTextModal` | ✅ Telegram-выгрузка |
| `dailyMeetings.ts`, `DailyReportView` | ✅ дейлики + read-only отчёт; **v0.26** — группировка похожих |
| `verify-daily-meetings.mjs` | ✅ проверка логики (без Vitest) |
| `ProjectsView` | ✅ отдельная вкладка |
| `MinimalView`, IPC `window-mode` | ✅ минимальное окно Electron |
| `UiIcon`, `public/icons/ui/` | ✅ иконки окна, закрыть, шевроны |
| `useSpeechRecognition`, `VoiceInputField` | ⚠️ голос — UI готов, в Electron нестабильно |
| `getDoneTasksGroupedByDate` | ✅ группировка истории |
| `assetUrl`, `resources/icon.png` | ✅ иконки в Electron (8 вкладок + UI) |
| `verify-icons.mjs`, `strip-icon-sources.mjs` | ✅ проверка и очистка при сборке |
| `attachmentStorage.ts`, `attachments-file.cjs` | ✅ фото на диске + IPC / dev API |
| `TaskAttachments`, `ImageLightbox` | ✅ загрузка, лайтбокс, Ctrl+V |
| `verify-attachments.mjs` | ✅ 12 проверок title/cleanup/normalize |
| `AmbientBackground.tsx` | ✅ базовые частицы v0.24; **v0.27** — тематические эффекты + `ambientPreset` |
| `index.css` + `data-palette` | ✅ скроллбары, `--bg-frost`; v0.27 — пульсация glow |
| Electron IPC (Jira) | шаблон для Ollama (v0.3) |
