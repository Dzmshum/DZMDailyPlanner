# Используем только pnpm в этом проекте
package-manager-strict=true

# Vite 8 импортирует rolldown напрямую — без hoist pnpm иногда не находит пакет
public-hoist-pattern[]=*rolldown*
public-hoist-pattern[]=*@rolldown/*
